export default function Home() {
    return (
        <main>
            <h1>universal-rate-limit Next.js Example</h1>
            <p>
                API routes at <code>/api/hello</code> and <code>/api/data</code> are rate-limited.
            </p>
        </main>
    );
}
